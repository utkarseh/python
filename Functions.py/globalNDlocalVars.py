total =10
def multiply(num1,num2):
    total=num1*num2
    return total
print(multiply(5,6))
total
#local variable only exists inside the function and cannot be accessed outside the function.
#global variable can be accessed anywhere in the program.