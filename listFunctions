shoppingList = ["milk", "eggs", "bread", "butter"]
del shoppingList[3]
print(shoppingList)
# del fxn delte an item from the list using index.
array1 = [1, 2, 3, 4, 5]
array2 = [6, 7, 8, 9, 10]
array3 = array1 + array2
print(array3)
length = len(array3)
print(length)
#len fxn is used to know the length of the list
numArray = (1000,1,-5,0.5,200)
maxvalue = max(numArray)
print(maxvalue)
minvalue = min(numArray)
print(minvalue)
#ax and min for max value and min value in the list
shoppingList.append("PEANUT BUTTER")
print(shoppingList)
#.append fxn is used to add an item to the list
count = shoppingList.count("milk")
print(count)
#.count fxn is used to count the number of times an item appears in the list